import React from "react";

const SectionName = ({ title }) => {
  return <div className="bg-neutral-800 uppercase h-fit text-nowrap left-0 absolute origin-top-left translate-y-[200%] text-sm -rotate-90 px-4 py-2 font-semibold">{title}</div>
};

export default SectionName;
