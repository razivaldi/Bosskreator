import React from "react";

const SectionName = ({ title }) => {
  return <div className="bg-neutral-800 uppercase h-fit text-nowrap left-0 absolute origin-top-left translate-y-[200%] text-sm -rotate-90 px-2 py-1 font-semibold">{title}</div>
};

export default SectionName;
